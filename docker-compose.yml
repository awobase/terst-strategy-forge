services:
  cayrib:
    build: .
    container_name: cayribepartners
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - cayrib-data:/app/data

volumes:
  cayrib-data:
